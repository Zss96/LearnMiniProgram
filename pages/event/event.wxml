<!--pages/event/event.wxml-->
<button bindtap="handleBtnclick" size="mini">按钮</button>
<button bind:tap="handleBtnClick" size="mini">按钮</button>
<button catch:tap="handleBtnClick" size="mini">按钮</button>
<view>常见事件</view>
<view>touchstart   touchmove  touchcancel  touchend  tap  longpress longtap</view>
<view class="box"
      bind:touchstart="handleTouchStart"
      bind:touchmove="handleTouchMove"
      bind:touchend="handleTouchEnd"
      bind:tap="handleTap"
      bind:longpress="handleLongPress"></view>

<!-- 3.事件对象的分析 -->

<button size="mini" 
        bindtap="handleEventClick"
        bindtouchend="handleEventEnd">事件对象</button>
<!-- touches和changedTouches -->
<view>touches用来记录当前有几个手指在小程序上触摸的</view>

<view>changedTouches用来记录手指的变化</view>

<view>currentTarget触发事件的view</view>
<view>target产生事件的view</view>
<view class="outer">
外层的view
  <view class="inner">内层的view</view>
</view>

<!-- 事件的传递参数 -->
<view class="container1">
  <block  wx:for="{{titles}}" wx:key="index">
    <view class="item" bindtap="handleItemClick"
    data-index="{{index}}"
    data-item="{{item}}">
      {{item}}
    </view>
  </block>
</view>

<!-- 5.事件冒泡和事件捕获 -->
<!-- bind:事件一层层的传递 -->
<!-- catch: 阻止事件的进一步传递 -->
<!-- capture-bind:tap事件捕获 -->